<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.smart.camera">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <data android:mimeType="com.ai.provider.dir/ai" />
            </intent-filter>
            <intent-filter>
                <data android:mimeType="com.ai.provider.item/ai" />
            </intent-filter>
        </activity>

        <provider
            android:authorities="com.ai.provider"
            android:name=".provider.AIDBProvider"/>
        <provider
            android:authorities="com.remove.provider"
            android:name=".provider.RemoveProvider"/>
        <provider
            android:authorities="com.upload.provider"
            android:name=".provider.UploadProvider"/>
        <provider
            android:authorities="com.instruction.provider"
            android:name=".provider.InstructionProvider"/>
    </application>

</manifest>